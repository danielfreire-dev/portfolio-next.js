/* "use client";
import LocaleSwitcherMobile from "./LocaleSwitcherMobile";
import NavigationList from "./NavigationList";

/**
 * Dock - A commented-out mobile dock navigation component.
 *
 * Intended to render a bottom-anchored navigation bar on small screens,
 * displaying navigation links and a locale switcher. Currently disabled
 * via a block comment.
 * /
const Dock = () => {
	return (
		<nav className="lg:hidden fixed z-20 bottom-0 left-0 w-screen bg-(--background)">
			<ul className="flex flex-row justify-evenly align-center capitalize">
				<NavigationList />
				<LocaleSwitcherMobile />
			</ul>
		</nav>
	);
};

export default Dock;
 */
