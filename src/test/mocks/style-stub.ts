/**
 * Style stub module used as a replacement for all .css imports during testing.
 * Exports an empty default object so that `import "./Foo.css"` resolves
 * without PostCSS processing.
 */
export default {};
